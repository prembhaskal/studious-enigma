## setup steps
install in local python environment
```bash
python3 -m venv  .venv
source .venv/bin/activate
pip install --upgrade pip
pip --version
pip install fastai torch torchvision torchaudio fastbook
pip install nbdev
```

## vscode
- install jupyter extension (you will get a prompt when you try to select a kernel)
- settings to load .env in environment (needed for keepings keys and stuff)